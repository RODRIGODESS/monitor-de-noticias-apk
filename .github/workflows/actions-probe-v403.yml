name: Actions probe v4.0.3

on:
  push:
    branches:
      - "codex/windows/v4.0.3-portable"
    paths:
      - ".github/workflows/actions-probe-v403.yml"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  probe:
    runs-on: ubuntu-latest
    timeout-minutes: 2
    steps:
      - name: Runner started
        run: echo "GitHub Actions runner iniciou normalmente"
